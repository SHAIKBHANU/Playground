#include<iostream>
using namespace std;
int main()
{
  //Type your code here.
  int len,r;
  cin>>r>>len;
  if(len>=2*r)
    cout<<"circle can be inside a square";
  else
    cout<<"circle cannot be inside a square";
}